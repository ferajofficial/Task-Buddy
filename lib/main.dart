import 'package:flutter/material.dart';
import 'package:task_buddy/const/app/app.dart';

Future<void> main() async {
  // final talker = Talker();
  // Bloc.observer = TalkerBlocObserver(talker: talker);

  runApp(const TaskBuddy());
}
